package com.Civil.Civil.entity;

import com.fasterxml.jackson.annotation.JsonManagedReference;

import javax.persistence.*;
import java.util.ArrayList;
import java.util.List;
/*
@Entity
@Table(name = "Tabla_Encuesta")
public class Encuesta {

    @Id // creamos un id
    @GeneratedValue(strategy = GenerationType.AUTO) // auto es para que se auto incremente cuando creamos un usuario
    @Column( name = "Id_Encuesta" , nullable = false,unique = true) // nombre de columna
    private Long id;

    @Column ( name = "Id_Usuario" , nullable)
    private String idUsuario;

    @Column ( name = "Id_pregunta" , nullable)
    private String idpregunta;

    @Column ( name = "Punta_Total" , nullable)
    private String puntajeTotal;

    @Column ( name = "Direccion" , nullable)
    private String direccion;



}

 */
